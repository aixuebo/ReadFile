一、ip地址由xxx.xxx.xxx.xxx组成。
每一个xxx都由一个字节8位组成,即存储的是127以内的数字。
因此可以4个字节,即32位的long组成。
同理ip6也是可以用long组成的。



ip转正整数后缺点:
1.每次查询固定ip的时候,需要进行转换。
2.不方便阅读,看数字不能定位到底是什么ip。

优点:
1.节省空间，用字符串存储ip,占用空间大,最少也需要7个字节,最多需要15个字节存储。
2.适合bitmap结构计算ip数量。

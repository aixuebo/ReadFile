一、算法中经常用到每一个user的存量信息,因为不是用户每天都有存量,但是要求存量每天都有,如果没有的话,使0补充
1.解决方法是sql关联日期表,这样日期内没有数据的,设置默认值为0即可,相当于sql中使用if语句
2.解决方法是在算法上进行if--else处理,但是要求时间要排序,这种方法不可取

二、补充数据不是默认值是0,而是用上一个值代替,那如何实现呢?
目前我只是在代码里面实现的，不知道sql的窗口函数是否可以满足